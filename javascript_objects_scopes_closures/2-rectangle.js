#!/usr/bin/node

// Defines a new Rectangle class that take 2 arguments

class Rectangle {
  constructor (w, h) {
    if (w > 0 && h > 0) {
      this.width = w;
      this.height = h;
    }
  }
}

module.exports = Rectangle;
